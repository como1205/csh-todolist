## 📋 설명

프론트엔드와 실제 배포된 백엔드 API를 연동하여 전체 사용자 시나리오를 테스트하고 버그를 수정합니다.

## ✅ Todo

- [ ] MVP 핵심 기능(회원가입-로그인-CRUD-로그아웃) 시나리오 통과
- [ ] 반응형 디자인 및 주요 브라우저 호환성 테스트 완료

## ✅ 완료 조건

### 1. E2E 테스트 시나리오 작성 및 실행

**시나리오 1: 사용자 인증 플로우**
1. 회원가입 페이지 접속
2. 유효한 정보로 회원가입
3. 로그인 페이지로 자동 리다이렉트 확인
4. 로그인
5. 메인 페이지로 리다이렉트 확인
6. 로그아웃
7. 로그인 페이지로 리다이렉트 확인

**시나리오 2: 할일 CRUD**
1. 로그인
2. 할일 추가 버튼 클릭
3. 모달에 정보 입력 및 저장
4. 할일 목록에 추가된 할일 확인
5. 할일 완료 체크박스 클릭
6. 완료 상태 변경 확인
7. 할일 수정 버튼 클릭
8. 모달에서 정보 수정 및 저장
9. 목록에 수정된 내용 반영 확인
10. 할일 삭제 버튼 클릭
11. 확인 다이얼로그 확인 및 삭제
12. 목록에서 제거 확인

**시나리오 3: 휴지통 기능**
1. 로그인 및 할일 삭제
2. 휴지통 페이지 이동
3. 삭제된 할일 목록 확인
4. 할일 복원 버튼 클릭
5. 휴지통 목록에서 제거 확인
6. 메인 페이지로 이동하여 복원된 할일 확인
7. 다시 휴지통으로 이동
8. 할일 영구 삭제 버튼 클릭
9. 확인 다이얼로그 확인
10. 휴지통 목록에서 제거 확인

**시나리오 4: 토큰 갱신**
1. 로그인
2. Access Token 만료까지 대기 (또는 개발자 도구에서 토큰 수동 만료)
3. API 호출 (할일 목록 조회 등)
4. 자동으로 토큰 갱신 및 요청 재시도 확인
5. 정상적으로 데이터 로드 확인

**시나리오 5: 에러 처리**
1. 잘못된 이메일/비밀번호로 로그인 시도 → 에러 메시지 확인
2. 중복 이메일로 회원가입 시도 → 에러 메시지 확인
3. 필수 필드 누락 폼 제출 → 유효성 검사 에러 확인
4. 네트워크 오류 시뮬레이션 → 에러 메시지 확인

### 2. 반응형 디자인 테스트

**테스트 디바이스/해상도**:
- 모바일: 375px, 414px
- 태블릿: 768px, 1024px
- 데스크톱: 1280px, 1920px

**체크 사항**:
- [ ] 모든 페이지가 각 해상도에서 정상 표시
- [ ] 버튼, 입력 필드 터치 영역 충분
- [ ] 텍스트 가독성 확보
- [ ] 모달 크기 적절
- [ ] 네비게이션 메뉴 모바일에서 적절히 표시

### 3. 브라우저 호환성 테스트

**테스트 브라우저**:
- Chrome (최신 버전)
- Firefox (최신 버전)
- Safari (최신 버전)
- Edge (최신 버전)

**체크 사항**:
- [ ] 모든 기능이 각 브라우저에서 정상 작동
- [ ] 레이아웃 깨짐 없음
- [ ] 스타일 일관성 유지

### 4. 성능 테스트

**체크 사항**:
- [ ] 페이지 로딩 속도 (3초 이내)
- [ ] API 응답 시간 (1초 이내)
- [ ] 할일 100개 로드 시 성능
- [ ] 메모리 누수 없음

### 5. 접근성 테스트

**체크 사항**:
- [ ] 키보드 네비게이션 가능
- [ ] 포커스 표시 명확
- [ ] 스크린 리더 호환성
- [ ] 색상 대비 충분 (WCAG 2.1 AA 기준)

### 6. 버그 수정

**발견된 모든 버그**:
- 심각도 분류 (Critical, High, Medium, Low)
- 재현 단계 문서화
- 수정 및 재테스트
- 회귀 테스트 (기존 기능 영향 확인)

### 7. 테스트 결과 문서화

- 테스트 케이스별 통과/실패 기록
- 발견된 버그 목록 및 수정 상태
- 스크린샷/비디오 증빙 자료
- 테스트 환경 정보 (OS, 브라우저, 버전 등)

## 🔧 기술적 고려사항

**E2E 테스트 도구 (선택)**:
- Playwright (추천)
- Cypress
- 수동 테스트

**Playwright 예시**:
```typescript
// tests/auth.spec.ts
import { test, expect } from '@playwright/test';

test('사용자 회원가입 및 로그인', async ({ page }) => {
  // 회원가입
  await page.goto('/register');
  await page.fill('input[name="email"]', 'test@example.com');
  await page.fill('input[name="password"]', 'Test1234!');
  await page.fill('input[name="confirmPassword"]', 'Test1234!');
  await page.fill('input[name="name"]', '테스트 사용자');
  await page.click('button[type="submit"]');

  // 로그인 페이지로 리다이렉트 확인
  await expect(page).toHaveURL('/login');

  // 로그인
  await page.fill('input[name="email"]', 'test@example.com');
  await page.fill('input[name="password"]', 'Test1234!');
  await page.click('button[type="submit"]');

  // 메인 페이지로 리다이렉트 확인
  await expect(page).toHaveURL('/');

  // 사용자 이름 표시 확인
  await expect(page.locator('text=테스트 사용자')).toBeVisible();
});
```

**반응형 테스트 (Playwright)**:
```typescript
test('모바일 반응형', async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 667 });
  await page.goto('/');
  // ... 테스트
});
```

**브라우저 호환성 테스트 설정**:
```typescript
// playwright.config.ts
export default {
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
    { name: 'webkit', use: { ...devices['Desktop Safari'] } },
  ],
};
```

**버그 보고 템플릿**:
```markdown
## 버그 제목

**재현 단계**:
1. ...
2. ...

**예상 동작**: ...

**실제 동작**: ...

**환경**:
- OS: Windows 11
- 브라우저: Chrome 120
- 화면 크기: 1920x1080

**스크린샷**: [첨부]

**심각도**: High
```

**주의사항**:
- 프론트엔드와 백엔드가 모두 실행 중이어야 함
- 테스트용 데이터베이스 사용 권장 (또는 테스트 후 데이터 정리)
- CORS 설정 확인
- 환경변수 설정 확인

**테스트 체크리스트**:
- [ ] 모든 E2E 시나리오 통과
- [ ] 모든 해상도에서 정상 표시
- [ ] 모든 브라우저에서 정상 작동
- [ ] Critical/High 버그 모두 수정
- [ ] 테스트 결과 문서화 완료

## 🔗 의존성

**선행 작업**:
- Phase 2 모든 백엔드 작업 완료
- Phase 3 모든 프론트엔드 작업 완료

**후행 작업**:
- Task 4.2: 백엔드/프론트엔드 프로덕션 배포

## 👤 담당

`test-automator`

## 📚 참고 문서

- docs/3-prd.md (전체 기능 명세)
- Playwright: https://playwright.dev/
- Cypress: https://www.cypress.io/
- WCAG 2.1 Guidelines: https://www.w3.org/WAI/WCAG21/quickref/
- Chrome DevTools: https://developer.chrome.com/docs/devtools/
